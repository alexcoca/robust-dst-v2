metadata: {}
reproduce:
  seed: 1234 # same seed for random, NumPy, PyTorch (CPU/GPU), across devices
  cudnn:
    enabled: True
    deterministic: False
    benchmark: True
linearization_version: 'version_1'
downsample_factor: 1
preprocessing:
  special_tokens:
  - '[user]'
  - '[system]'
  - '[slot]'
